샘플 데이터 

크롤링은
국비에서 대부분 컨트롤러에서 가져감

SQL은 하나의 쿼리 랭귀지이므로 거의 다 함수화되어있다.

★ 괄호가 있으면 함수다
COUNT() : 집계함수
	COUNT(*) : 전체 데이터(로우)의 갯수를 셈
	COUNT(NUM) : NUM의 갯수를 셈

함수를 그냥 사용하면 프로그램이 작동은 하지만
 불러올 수가 없어서 JAVA에서 resultset을 통해 불러올 수가 없다
AS 별칭으로 

모델에서 TOTAL 데이터(cnt) 를 넣은 것을 ctrl에서 알아야 된다.
  == 멤버 변수를 넣어야 한다.

프로그램을 작성할 때 내 생각을 빠짐없이 쓰는 것이 좋다.

--------
샘플 데이터 INSERT 하면서 app.start();
샘플 데이터가 이미 있을 때 막기

전체 출력 SELECT ALL

재고 랜덤으로 INSERT

이름 검색
정렬

전달하거나 사용하는 것은
1기능 1개의 new

반환받는 것은 new를 사용하지 않아도 된다.

LIKE %>%
LIKE %'?'% (자바에서)
오라클에서 문자열을 연결하는 방법

?는 따옴표(' ') 전체를 들고 옴 (? == '?')
java의 +와 오라클의 ||은 같은 의미로 사용
LIKE %||?||%
